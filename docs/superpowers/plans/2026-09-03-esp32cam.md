# ESP32-CAM 摄像头测试模块 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在后台配置页内嵌摄像头测试弹窗，完成发现→直播→拍照→分辨率控制的 H5 可验收闭环。

**Architecture:** 纯函数协议层可单测 + 薄封装发现/服务层 + settings.vue 内 modal UI；H5 用 `<img>` 直播 MJPEG，APP-PLUS 才走 UDPSocket，stream.html 中转页留到第二步。

**Tech Stack:** Vue 3 `<script setup>` / uni-app H5 + APP-PLUS / `uni.request` + `uni.downloadFile` / `node:test` + `node:assert/strict`

## Global Constraints

- 两空格缩进，单引号 JS 字符串，ES modules，无分号，Vue `<script setup>` 风格
- 路由不新增 tab，不改 `src/pages.json` tabBar，只动 settings modal
- ESP32 协议按 spec 不改：`ESP32CAM|IP=..|PORT=..|STREAM=..|CAPTURE=..|MAC=..|NAME=..`，UDP 4210，`DISCOVER_ESP32CAM`
- 超时 5s、失败重试 1 次、设备 5s 无广播判离线
- 测试：`node --test <file>`， colocated `*.test.mjs`

---

### Task 1: cameraProtocol 纯函数 + 单测

**Files:**
- Create: `src/utils/cameraProtocol.js`
- Test: `src/utils/cameraProtocol.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `parseEsp32CamMessage(msg)`, `normalizeManualIp(input)`, `buildControlUrl(base,var,val)`, `buildCaptureUrl(base)`, `buildStreamUrl(base)`, `buildStatusUrl(base)`, `guessBroadcastAddresses()`, `FRAMESIZE_MAP`

- [ ] **Step 1: Write the failing test**

```js
import { describe, it } from 'node:test'
import assert from 'node:assert/strict'
import { parseEsp32CamMessage, normalizeManualIp, buildControlUrl, guessBroadcastAddresses } from './cameraProtocol.js'

describe('cameraProtocol', () => {
  it('解析标准广播消息', () => {
    const msg = 'ESP32CAM|IP=192.168.223.102|PORT=80|STREAM=http://192.168.223.102:81/stream|CAPTURE=http://192.168.223.102/capture|MAC=AA:BB:CC:DD|NAME=esp32cam'
    const dev = parseEsp32CamMessage(msg)
    assert.equal(dev.IP, '192.168.223.102')
    assert.equal(dev.PORT, '80')
    assert.equal(dev.STREAM, 'http://192.168.223.102:81/stream')
    assert.equal(dev.MAC, 'AA:BB:CC:DD')
  })
  it('非协议消息返回 null', () => {
    assert.equal(parseEsp32CamMessage('HELLO'), null)
    assert.equal(parseEsp32CamMessage(''), null)
  })
  it('手动 IP 归一化', () => {
    assert.equal(normalizeManualIp('192.168.223.102'), 'http://192.168.223.102')
    assert.equal(normalizeManualIp('http://192.168.4.1/'), 'http://192.168.4.1')
  })
  it('control URL 拼装', () => {
    assert.equal(buildControlUrl('http://192.168.4.1', 'framesize', 8), 'http://192.168.4.1/control?var=framesize&val=8')
  })
  it('广播地址猜测含兜底', () => {
    const list = guessBroadcastAddresses()
    assert.ok(list.includes('255.255.255.255'))
    assert.ok(list.includes('192.168.223.255'))
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test src/utils/cameraProtocol.test.mjs`
Expected: FAIL with "Cannot find module" / "function not defined"

- [ ] **Step 3: Write minimal implementation**

```js
export const DISCOVER_MSG = 'DISCOVER_ESP32CAM'
export const DISCOVER_PORT = 4210
export const FRAMESIZE_MAP = [
  { label: 'SVGA 800x600', value: 8 },
  { label: 'XGA 1024x768', value: 9 },
  { label: 'UXGA 1600x1200', value: 10 }
]

function stripBase(input) {
  return String(input || '').trim().replace(/\/+$/, '')
}

export function parseEsp32CamMessage(msg) {
  const text = String(msg || '').trim()
  if (!text.startsWith('ESP32CAM|')) return null
  const out = {}
  text.split('|').slice(1).forEach((part) => {
    const idx = part.indexOf('=')
    if (idx <= 0) return
    const k = part.slice(0, idx).trim()
    const v = part.slice(idx + 1).trim()
    if (k) out[k] = v
  })
  if (!out.IP) return null
  return out
}

export function normalizeManualIp(input) {
  const raw = String(input || '').trim()
  if (!raw) return ''
  const withProto = /^https?:\/\//i.test(raw) ? raw : `http://${raw}`
  return stripBase(withProto)
}

export function buildControlUrl(base, variable, value) {
  return `${stripBase(base)}/control?var=${encodeURIComponent(variable)}&val=${encodeURIComponent(value)}`
}

export function buildCaptureUrl(base) {
  return `${stripBase(base)}/capture`
}

export function buildStreamUrl(base) {
  const b = stripBase(base)
  return `${b}:81/stream`
}

export function buildStatusUrl(base) {
  return `${stripBase(base)}/status`
}

export function guessBroadcastAddresses() {
  return ['255.255.255.255', '192.168.223.255', '192.168.43.255', '192.168.4.255']
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test src/utils/cameraProtocol.test.mjs`
Expected: PASS, 5 tests

- [ ] **Step 5: Commit**

```bash
git add src/utils/cameraProtocol.js src/utils/cameraProtocol.test.mjs
git commit -m "feat: 新增 ESP32-CAM 协议解析与 URL 拼装纯函数"
```

### Task 2: discovery 封装 + cameraStore

**Files:**
- Create: `src/utils/cameraDiscovery.js`
- Create: `src/stores/cameraStore.js`

**Interfaces:**
- Consumes: `parseEsp32CamMessage, guessBroadcastAddresses, DISCOVER_MSG, DISCOVER_PORT` from Task 1
- Produces: `createDiscovery({onDevice})` with `{supported, start(), stop()}`, `cameraStore` with `{devices, upsertDevice(raw), pruneOffline(now), clear()}`

- [ ] **Step 1: Write minimal implementation (无单测，手工验收；store 逻辑由 Task1 已覆盖解析)**

```js
import { reactive } from 'vue'
import { parseEsp32CamMessage } from '../utils/cameraProtocol.js'

const OFFLINE_MS = 5000
export const devices = reactive([])

export function upsertDevice(raw) {
  if (!raw || !raw.IP) return null
  const key = raw.MAC || raw.IP
  const now = Date.now()
  const found = devices.find((d) => (d.MAC || d.IP) === key)
  const record = {
    MAC: raw.MAC || '',
    IP: raw.IP,
    STREAM: raw.STREAM || `http://${raw.IP}:81/stream`,
    CAPTURE: raw.CAPTURE || `http://${raw.IP}/capture`,
    NAME: raw.NAME || 'esp32cam',
    lastSeen: now
  }
  if (found) Object.assign(found, record)
  else devices.push(record)
  return record
}

export function pruneOffline(now = Date.now()) {
  for (let i = devices.length - 1; i >= 0; i -= 1) {
    if (now - devices[i].lastSeen > OFFLINE_MS) devices.splice(i, 1)
  }
}

export function clear() {
  devices.splice(0, devices.length)
}

export const cameraStore = { devices, upsertDevice, pruneOffline, clear }
```

```js
import { DISCOVER_MSG, DISCOVER_PORT, guessBroadcastAddresses, parseEsp32CamMessage } from './cameraProtocol.js'

export function createDiscovery({ onDevice } = {}) {
  const isAppPlus = typeof uni !== 'undefined' && uni.createUDPSocket
  if (!isAppPlus) return { supported: false, start() {}, stop() {} }
  let socket = null
  let timer = null
  function sendOnce() {
    const targets = guessBroadcastAddresses()
    targets.forEach((address) => {
      try {
        socket.send({ address, port: DISCOVER_PORT, message: DISCOVER_MSG })
      } catch (err) {}
    })
  }
  return {
    supported: true,
    start() {
      socket = uni.createUDPSocket()
      socket.onMessage((res) => {
        const text = res && res.message ? String(res.message) : ''
        const dev = parseEsp32CamMessage(text)
        if (dev && onDevice) onDevice(dev)
      })
      try { socket.bind(DISCOVER_PORT) } catch (err) {}
      sendOnce()
      timer = setInterval(sendOnce, 2000)
    },
    stop() {
      if (timer) clearInterval(timer)
      timer = null
      try { socket && socket.close() } catch (err) {}
      socket = null
    }
  }
}
```

- [ ] **Step 2: H5 启动无报错**

Run: `npm run dev:h5 -- --port 5175` 打开后台配置页
Expected: 控制台无 `createUDPSocket` 报错（H5 返回 supported:false）

- [ ] **Step 3: Commit**

```bash
git add src/utils/cameraDiscovery.js src/stores/cameraStore.js
git commit -m "feat: 新增摄像头发现封装与设备 store（5s 离线判定）"
```

### Task 3: esp32cam 服务层（status/capture/control）

**Files:**
- Create: `src/services/esp32cam.js`

**Interfaces:**
- Consumes: `buildControlUrl/buildCaptureUrl/buildStatusUrl` from Task 1
- Produces: `fetchCamStatus(base)`, `sendCamControl(base, variable, value)`, `capturePhoto(base)` → `{ tempFilePath }`

- [ ] **Step 1: Write minimal implementation**

```js
import { buildCaptureUrl, buildControlUrl, buildStatusUrl } from '../utils/cameraProtocol.js'

function uniRequest({ url, timeout = 5000 }) {
  return new Promise((resolve, reject) => {
    uni.request({ url, method: 'GET', timeout, success: resolve, fail: reject })
  })
}

async function withRetry(fn, retries = 1) {
  let lastErr = null
  for (let i = 0; i <= retries; i += 1) {
    try {
      return await fn()
    } catch (err) {
      lastErr = err
    }
  }
  throw lastErr
}

export function fetchCamStatus(base) {
  return withRetry(async () => {
    const res = await uniRequest({ url: buildStatusUrl(base) })
    if (res.statusCode < 200 || res.statusCode >= 300) throw new Error(`状态读取失败：HTTP ${res.statusCode}`)
    return res.data
  })
}

export function sendCamControl(base, variable, value) {
  return withRetry(async () => {
    const res = await uniRequest({ url: buildControlUrl(base, variable, value) })
    if (res.statusCode < 200 || res.statusCode >= 300) throw new Error(`控制下发失败：HTTP ${res.statusCode}`)
    return res.data
  })
}

export function capturePhoto(base) {
  return withRetry(() => new Promise((resolve, reject) => {
    uni.downloadFile({ url: buildCaptureUrl(base), timeout: 5000, success: (res) => {
      if (res.statusCode === 200 && res.tempFilePath) resolve(res)
      else reject(new Error(`拍照失败：HTTP ${res.statusCode}`))
    }, fail: reject })
  }))
}
```

- [ ] **Step 2: Commit**

```bash
git add src/services/esp32cam.js
git commit -m "feat: 新增 ESP32-CAM 状态/控制/拍照服务（5s 超时+重试）"
```

### Task 4: settings.vue 摄像头测试 modal

**Files:**
- Modify: `src/pages/settings/settings.vue`（menu-card + `activeModal==='camera'` 分支 + script 逻辑 + scoped 样式复用现有 modal/menu 风格）

**Interfaces:**
- Consumes: Task 1/2/3 的 `normalizeManualIp/buildStreamUrl/FRAMESIZE_MAP/cameraStore/createDiscovery/fetchCamStatus/sendCamControl/capturePhoto`
- Produces: UI：发现列表 + 手动输入 + `<img>` 直播 + 拍照 + framesize/quality/flash 设置

- [ ] **Step 1: Template — admin menu-grid 内新增卡片（放在数据调试卡片后）**

```html
<view class="menu-card camera-action" @tap="openCamera">
  <view class="menu-icon-wrap">
    <AppIcon name="camera" :size="34" />
  </view>
  <view class="menu-copy">
    <text class="menu-title">摄像头测试</text>
    <text class="menu-desc">ESP32-CAM 发现 / 直播 / 拍照</text>
  </view>
  <text class="menu-arrow">测试</text>
</view>
```

modal body 内（`v-else-if="activeModal === 'camera'"`）：发现状态行 + 设备列表 + 手动输入行 + 直播 `<img :src="camStreamUrl">` + 拍照/刷新按钮 + framesize picker + quality input + flash switch。若 `AppIcon` 无 camera 图标则复用 `sliders-horizontal`，不新增图标字体。

- [ ] **Step 2: Script — refs + openCamera/close + discovery 生命周期**

```js
const camManualIp = ref('')
const camBase = ref('')
const camStreamUrl = ref('')
const camStatusText = ref('')
const camFramesize = ref(8)
const camQuality = ref(12)
const camFlash = ref(false)
let camDiscovery = null
let camPruneTimer = null

function openCamera() {
  activeModal.value = 'camera'
  const { createDiscovery } = requireDiscovery()
  camDiscovery = createDiscovery({ onDevice: (dev) => cameraStore.upsertDevice(dev) })
  camDiscovery.start()
  camPruneTimer = setInterval(() => cameraStore.pruneOffline(), 2000)
}
```

H5 无 `require`，用顶层 `import { createDiscovery } from '../../utils/cameraDiscovery'` 与 `import { cameraStore } from '../../stores/cameraStore'`。`connectManual()` 用 `normalizeManualIp(camManualIp)` 设置 `camBase/camStreamUrl`。`takePhoto()` 调 `capturePhoto` 后 `uni.saveImageToPhotosAlbum`，H5 降级 `window.open(url)`。`applyFramesize/Quality/Flash()` 调 `sendCamControl`。关闭 modal 时 `stop()` 并清 timer。

- [ ] **Step 3: H5 手工验收**

Run: `npm run dev:h5 -- --port 5175`
Expected: 后台配置 → 摄像头测试 → 手动输入 `192.168.4.1` → 出流；断网时 toast 提示

- [ ] **Step 4: Commit**

```bash
git add src/pages/settings/settings.vue
git commit -m "feat: 后台配置新增摄像头测试弹窗（发现/直播/拍照）"
```

### Task 5: manifest 权限 + 回归验证

**Files:**
- Modify: `src/manifest.json`
- Test: 运行既有 suites

- [ ] **Step 1: manifest android 权限补齐 + 明文 + iOS ATS**

```json
"android": {
  "permissions": [
    "<uses-permission android:name=\"android.permission.INTERNET\"/>",
    "<uses-permission android:name=\"android.permission.ACCESS_WIFI_STATE\"/>",
    "<uses-permission android:name=\"android.permission.CHANGE_WIFI_MULTICAST_STATE\"/>",
    "<uses-permission android:name=\"android.permission.ACCESS_FINE_LOCATION\"/>"
  ],
  "usesCleartextTraffic": true
},
"ios": {
  "NSAppTransportSecurity": { "NSAllowsArbitraryLoads": true }
}
```

以实际 `manifest.json` 结构为准做最小 merge，不动 splashscreen/compiler 配置。

- [ ] **Step 2: Run tests**

Run: `node --test src/utils/cameraProtocol.test.mjs src/utils/configImportExport.test.mjs scripts/package-scripts.test.mjs scripts/lib/config.test.mjs scripts/lib/email-sender.test.mjs`
Expected: ALL PASS

- [ ] **Step 3: Run H5 build**

Run: `npm run build:h5`
Expected: 构建成功，无新增 warning（除既有 uni 统计提示）

- [ ] **Step 4: Commit**

```bash
git add src/manifest.json
git commit -m "feat: 补齐摄像头明文与 WiFi/定位权限"
```

## Self-Review

- spec 覆盖：广播解析/手动兜底/control映射/5s离线/5s超时重试/H5降级/权限/EC second-step 预留均有任务
- 无占位符：每步含真实代码与确切命令
- 类型一致：`parseEsp32CamMessage` 返回含 `IP/STREAM/CAPTURE/MAC/NAME`，store 与 UI 字段一致
