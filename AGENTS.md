# Project Instructions

- Do not build or package APKs after every code change. Only run APK packaging when the user explicitly asks for it.
- When submitting to GitHub, default to committing directly to `master` unless the user explicitly asks to create a new branch and submit via pull request.
